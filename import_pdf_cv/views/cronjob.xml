<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="ir_cron_process_pdfs" model="ir.cron">
            <field name="name">Process uploaded candidate PDFs</field>
            <field name="model_id" ref="base.model_ir_attachment"/>
            <field name="state">code</field>
            <field name="code">model._cron_process_pending_attachments(limit=6)</field>
            <field name="interval_number">4</field> <!-- every 4 min. -->
            <field name="interval_type">minutes</field>


        </record>


    </data>
</odoo>